# Loop 2 integration review

After verified unit branches integrate into `next`, the evaluation lane boots the
assembled app and independently checks the combined behavior. A failure creates
isolated build-lane repair worktrees. Every repair is re-integrated and the
evaluation lane runs again. No model tier is inferred from the defect: the
session contract remains authoritative.
